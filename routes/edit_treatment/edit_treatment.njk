{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('edit_treatment.title') }}</h1>

    <div>
        <p>
            {{ __('edit_treatment.intro') }}
        </p>

        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.firstname', 'form.firstname.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
