{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('delete_condition.title') }}</h1>

    <div>
        <p>
            {{ __('delete_condition.intro') }}
        </p>

        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.firstname', 'form.firstname.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
